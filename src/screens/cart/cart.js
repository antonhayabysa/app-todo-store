import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Cart = () => {
  return (
    <SafeAreaView>
      <Text>Cart</Text>
    </SafeAreaView>
  );
};

export default Cart;
